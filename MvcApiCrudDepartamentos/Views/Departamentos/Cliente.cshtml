﻿<h1>Api Departamentos Cliente</h1>

@section Scripts {
    <script src="~/js/serviceapidepartamentos.js"></script>
    <script>
        $(document).ready(function(){
            loadDepts();

            $("#btnpost").click(function (){
                let id = $("#cajaid").val();
                let nombre = $("#cajanombre").val();
                let localidad = $("#cajalocalidad").val();

                postDeptAsync(id, nombre, localidad, function(){
                    loadDepts();
                })
            })

            $("#btnput").click(function (){
                let id = $("#cajaid").val();
                let nombre = $("#cajanombre").val();
                let localidad = $("#cajalocalidad").val();

                putDeptAsync(id, nombre, localidad, function(){
                    loadDepts();
                })
            })

            $("#btndelete").click(function (){
                let id = $("#cajaid").val();

                deleteDeptAsync(id, function(){
                    loadDepts();
                })
            })
        })

        function loadDepts(){
            getDeptsAsync(function(data){
                var html = "";
                $.each(data, function(index, departamento){
                    html += "<tr>";
                        html += "<td>"+ departamento.idDepartamento +"</td>";
                        html += "<td>"+ departamento.nombre +"</td>";
                        html += "<td>"+ departamento.localidad +"</td>";
                    html += "</tr>";
                })
                $("#tabladepts tbody").html(html)
            })
        }

    </script>
}

<form>
    <label>Id Dept</label>
    <input type="text" id="cajaid" class="form-control"/>
    <br/>
    <label>Nombre</label>
    <input type="text" id="cajanombre" class="form-control" />
    <br />
    <label>Localidad</label>
    <input type="text" id="cajalocalidad" class="form-control" />
    <br />
    <button id="btnpost" type="button" class="form-control" style="background-color:lightcyan">Post</button>
    <button id="btnput" type="button" class="form-control" style="background-color:lightcyan">Put</button>
    <button id="btndelete" type="button" class="form-control" style="background-color:lightcyan">Delete</button>
    <br/>
</form>

<table id="tabladepts" class="table table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Localidad</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>